# Temper Syntax Definitions

This currently targets just Pygments, but we could potentially combine some
definitions with the VSCode/TextMate grammar sometime.

Subdirs:

- `temper-pygments` - Explicit Pygments support for Temper, written in Python
- `temper-syntax` - Core definitions in Temper, generating support for
  temper-pygments.
